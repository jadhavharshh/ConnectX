{
  "name": "pythonserver",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 main.py",
    "start": "python3 main.py",
    "build": "echo 'No build step for Python server'",
    "lint": "echo 'No linting configured for Python server'",
    "check-types": "echo 'No type checking configured for Python server'",
    "install-deps": "pip3 install -r requirements.txt"
  },
  "dependencies": {},
  "devDependencies": {}
}
